import { Component } from '@angular/core';

@Component({
  standalone: true,
  template: `
    <h1>Networks</h1>
    <p>TCP, DNS, HTTP, routing, and other network protocols.</p>
  `,
})
export class NetworksComponent {}
